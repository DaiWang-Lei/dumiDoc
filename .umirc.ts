import { defineConfig } from 'dumi';
export default defineConfig({
  title: 'LingoScript',
  favicon: '/imgs/logo.png',
  logo: '/imgs/logo.png',
  outputPath: 'docs-dist',
  links: [{ rel: 'icon', href: '/imgs/logo.png' }],
  description: '编造自己的元宇宙',
  // mode:'site',
  // more config: https://d.umijs.org/config
});
